// Sponsors & Exhibitors are already built directly and elegantly inside the single-page multi-tab layout in Home.tsx.
// This is a helper component to expose subpage tabs cleanly if we want to expand routing,
// but the client-side single-page app handles everything inside Home.tsx for flawless state management.
export default function SponsorsExhibitors() {
  return null;
}
